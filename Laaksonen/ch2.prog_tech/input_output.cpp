#include <bits/stdc++.h>

using namespace std;


int main() {
  // these two lines makes the io faster
  ios::sync_with_stdio(0);
  cin.tie(0);

  int a, b;
  string x;
  /* a demo for stdin
  cin >> a >> b >> x;
  */
  a = 123, b = 456, x = "monkey";
  cout << a << " " << b << " " << x << "\n";

  /* re direct files as input and output
  freopen("input.txt", "r", stdin);
  freopen("output.txt", "w", stdout);
  */
  return 0;
}